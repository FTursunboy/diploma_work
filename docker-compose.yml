version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: book-parser-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./storage:/app/storage
